[package]
name = "tik"
version = "0.1.0"
edition = "2024"
description = "Count LLM tokens in text files"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
tiktoken = "3.1.2"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
